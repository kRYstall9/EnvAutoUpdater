{
  "branches": ["main"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    ["@semantic-release/exec", {
      "prepareCmd": "echo ${nextRelease.version} > .version",
      "publishCmd": "docker build -t ${process.env.DOCKERHUB_USERNAME}/envautoupdater:${nextRelease.version} -t ${process.env.DOCKERHUB_USERNAME}/envautoupdater:latest . && docker push ${process.env.DOCKERHUB_USERNAME}/envautoupdater:${nextRelease.version} && docker push ${process.env.DOCKERHUB_USERNAME}/envautoupdater:latest"
    }],
    ["@semantic-release/git", {
      "assets": ["CHANGELOG.md"],
      "message": "chore(release): ${nextRelease.version} [skip ci]\n\n${nextRelease.notes}"
    }],
    "@semantic-release/github"
  ]
}
